version: v1

mode: file

plans:
  all:

groups:
  pi:
    nux: "~/.pi/agent"
    mac: "~/.pi/agent"
  fish:
    nux: "~/.config/fish"
    mac: "~/.config/fish"
  ghostty:
    nux: "~/.config/ghostty"
    mac: "~/.config/ghostty"
  git:
    nux: "~/.config/git"
    mac: "~/.config/git"
  jj:
    nux: "~/.config/jj"
    mac: "~/.config/jj"
  zed:
    nux: "~/.config/zed"
    mac: "~/.config/zed"
