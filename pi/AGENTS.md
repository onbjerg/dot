# Global Agent Instructions

- Always use `jj` over `git` if available.
- When creating or naming Git branches or jj bookmarks, prefix the name with `onbjerg/`.
- PR descriptions should be concise and explain what they change/fix and any references for correctness if necessary. Do not include `## Summary` and `## Validation` sections.
